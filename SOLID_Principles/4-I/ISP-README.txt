SOLID
I = Interface Segregation Principle

*Have many specific interfaces are better than a single, so classes consumes only on what they need
*Avoid unnecessary code/methods without implementation on classes which implements interface
*Easily maintainance